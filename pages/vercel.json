{
  "buildCommand": "cd \"../Ledger design library\" && npm install && cd ../pages && npm run build",
  "installCommand": "npm install",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
